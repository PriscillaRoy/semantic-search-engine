# Production (Linux x86_64 — Render/Cloud Run)
# Only direct dependencies — let pip resolve transitive deps

# API
fastapi==0.115.0
uvicorn==0.30.6
uvicorn-worker==0.3.0
gunicorn==21.2.0
pydantic==2.13.4
python-dotenv==1.2.1

# ML — no faiss-cpu needed in production (SEARCH_BACKEND=milvus)
sentence-transformers==4.1.0
numpy>=2.0.0
torch==2.7.0
scikit-learn==1.6.1

# Graph
networkx==3.2.1

# Database
psycopg2-binary==2.9.12
supabase==2.31.0

# Cache
redis==5.0.1

# Vector DB
pymilvus==3.0.0

# Feature store
feast==0.63.0
pandas==2.2.3
pyarrow==24.0.0

# AWS (Feast needs it)
boto3==1.38.29

# Utilities
requests==2.32.3
httpx>=0.27.0
tqdm==4.67.1
PyYAML==6.0.2